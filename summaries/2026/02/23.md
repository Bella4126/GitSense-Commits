# Activity Summary for 2/23/2026

## 1:36:43 PM
The code changes log primarily reflects the initial setup and subsequent extensive corrections within a Spring Boot backend application named `rakshak-backend`. All documented changes occurred on **2/23/2026**.

**File-Specific Updates:**

*   **`c:\Users\ss\rakshak-backend\src\main\java\com\rakshak\rakshak_backend\model\Document.java` (12:45 PM):**
    *   This file defines a JPA entity for `Document` (representing uploaded study materials) with fields like `id`, `title`, `fileUrl`, `uploadedBy` (a `User`), and `createdAt`.
    *   An initial entry contained a duplicate package declaration (`package com.rakshak.rakshak_backend.model; package com.rakshak.backend.model;`), which was quickly corrected in a subsequent entry.
*   **`c:\Users\ss\rakshak-backend\src\main\java\com\rakshak\rakshak_backend\dto\RegisterRequest.java` (12:48 PM):**
    *   Defines a Data Transfer Object (DTO) for user registration requests, including fields for `name`, `email`, `password`, and optional `examType` and `city`, all with `jakarta.validation` constraints.
*   **`c:\Users\ss\rakshak-backend\src\main\java\com\rakshak\rakshak_backend\dto\LoginRequest.java` (12:48 PM):**
    *   Defines a DTO for user login requests, containing `email` and `password` with validation constraints.
*   **`c:\Users\ss\rakshak-backend\src\main\java\com\rakshak\rakshak_backend\dto\AuthResponse.java` (12:49 PM - 12:50 PM):**
    *   Defines a DTO for authentication responses, including the JWT `token`, `name`, `email`, and `role` of the authenticated user. No functional changes observed between the two entries for this file.
*   **`c:\Users\ss\rakshak-backend\src\main\java\com\rakshak\rakshak_backend\repository\UserRepository.java` (12:50 PM):**
    *   Defines a Spring Data JPA repository for the `User` entity, extending `JpaRepository`. It includes methods `findByEmail` and `existsByEmail` for authentication purposes.
    *   Similar to `Document.java`, it initially had a duplicate package declaration and an incorrect import path for the `User` model, which were rectified in subsequent entries within seconds.
*   **`c:\Users\ss\rakshak-backend\src\main\java\com\rakshak\rakshak_backend\repository\DocumentRepositor.java` (12:52 PM) and `...DocumentRepository.java` (12:53 PM):**
    *   The initial entry introduced `DocumentRepositor.java` with a typo in the filename and an incorrect `Document` import.
    *   This was quickly corrected by renaming the file to `DocumentRepository.java` and then fixing the `Document` import path in a follow-up commit.
*   **`c:\Users\ss\rakshak-backend\src\main\java\com\rakshak\rakshak_backend\config\JwtAuthFilter.java` (12:55 PM - 12:57 PM):**
    *   This file's log shows a significant evolution: it initially contained the code for `JwtUtil.java` (possibly a copy-paste error or misnaming), then became an empty file, and finally, by **12:57:09 PM**, it was correctly populated with the `JwtAuthFilter` class. This filter intercepts HTTP requests to validate JWT tokens using `JwtUtil` and sets the user's authentication context in Spring Security.
*   **`c:\Users\ss\rakshak-backend\src\main\java\com\rakshak\rakshak_backend\config\JwtUtil.java` (12:55 PM):**
    *   This file defines the `JwtUtil` class, responsible for all JWT operations, including generating, validating, and parsing tokens, extracting claims (like username/email), and managing token expiration. It retrieves the secret key and expiration from application properties.
*   **`c:\Users\ss\rakshak-backend\src\main\java\com\rakshak\rakshak_backend\config\SecurityConfig.java` (12:57 PM):**
    *   This configuration class, initially empty, was quickly populated to set up Spring Security. It disables CSRF, specifies public (`/api/auth/**`) and authenticated routes, configures stateless session management, registers `JwtAuthFilter`, defines a `UserDetailsService` (using `UserRepository`), sets up `DaoAuthenticationProvider` with `BCryptPasswordEncoder`, and exposes the `AuthenticationManager`.
    *   A series of rapid corrections addressed an initial duplicate package declaration and incorrect import paths for `UserRepository`.
*   **`c:\Users\ss\rakshak-backend\src\main\java\com\rakshak\rakshak_backend\config\WebSocketConfig.java` (1:02 PM):**
    *   Defines the `WebSocketConfig` for real-time features (noted as a placeholder for Phase 2). It configures a STOMP message broker with `/topic` for subscriptions and `/app` for client messages, registering `/ws` as the endpoint.
*   **`c:\Users\ss\rakshak-backend\src\main\java\com\rakshak\rakshak_backend\service\AuthService.java` (1:03 PM - 1:04 PM):**
    *   Implements the core authentication logic for `register` and `login`. The `register` method hashes passwords and generates a JWT, while `login` authenticates users via `AuthenticationManager` and issues a JWT.
    *   This file underwent a lengthy sequence of changes, correcting multiple incorrect import statements and a duplicate package declaration incrementally over several commits.
*   **`c:\Users\ss\rakshak-backend\src\main\java\com\rakshak\rakshak_backend\service\UserService.java` (1:05 PM - 1:06 PM):**
    *   A placeholder service for user profile management (Phase 2). It contains a `getUserById` method.
    *   Similar to `AuthService`, this file saw a series of corrections for duplicate package declarations and incorrect import paths for `User` and `UserRepository`.
*   **`c:\Users\ss\rakshak-backend\src\main\java\com\rakshak\rakshak_backend\service\PostService.java` (1:06 PM - 1:07 PM):**
    *   A placeholder service for post management (Phase 2).
    *   Required corrections for duplicate package declarations and incorrect `PostRepository` imports.
*   **`c:\Users\ss\rakshak-backend\src\main\java\com\rakshak\rakshak_backend\service\CommentService.java` (1:07 PM):**
    *   A placeholder service for comment management (Phase 2).
    *   Required corrections for incorrect `CommentRepository` imports.
*   **`c:\Users\ss\rakshak-backend\src\main\java\com\rakshak\rakshak_backend\service\DocumentService.java` (1:35 PM - 1:36 PM):**
    *   A placeholder service for document management (Phase 2).
    *   Required corrections for incorrect `DocumentRepository` imports.

**Timestamps of Significant Changes:**

The entire log spans roughly one hour, from **12:45 PM to 1:36 PM** on **2/23/2026**. This period was characterized by rapid development and immediate correction of foundational architectural issues.

*   **12:45 PM - 12:53 PM:** Initial definition of core models, DTOs, and repositories.
*   **12:55 PM - 1:02 PM:** Setup of JWT-based authentication (`JwtUtil`, `JwtAuthFilter`) and Spring Security configuration (`SecurityConfig`), along with WebSocket configuration.
*   **1:03 PM - 1:07 PM:** Implementation of authentication service logic and placeholder service definitions (`AuthService`, `UserService`, `PostService`, `CommentService`).
*   **1:35 PM - 1:36 PM:** Final minor corrections for `DocumentService`.

**Patterns and Recurring Elements:**

1.  **Package Name Consistency:** The project consistently uses `com.rakshak.rakshak_backend` as its base package. Many initial commits across different files introduced errors where package declarations or import statements incorrectly referenced `com.rakshak.backend` or had duplicate `package` statements, indicating a potential refactoring or copy-paste issue that was systematically fixed.
2.  **Lombok Usage:** Lombok annotations (`@Data`, `@Builder`, `@AllArgsConstructor`, `@NoArgsConstructor`, `@RequiredArgsConstructor`) are heavily used across model, DTO, and service classes to reduce boilerplate code, especially for getters, setters, constructors, and builder patterns.
3.  **Spring Boot Architecture:** The changes establish a typical Spring Boot REST API structure with JPA entities, DTOs, Spring Data JPA repositories, Spring `Service` components, and Spring Security for authentication.
4.  **JWT-based Authentication:** A clear pattern for implementing JWT authentication is present, involving a `JwtUtil` for token handling, a `JwtAuthFilter` for request interception, and `SecurityConfig` to integrate these into the Spring Security chain.
5.  **Iterative Development & Placeholders:** Numerous comments like "Placeholder for Phase 2" appear in several files (e.g., `Document.java`, `DocumentRepository.java`, `WebSocketConfig.java`, `UserService.java`, `PostService.java`, `CommentService.java`, `DocumentService.java`). This indicates that the current phase focuses on setting up the basic architecture, security, and data access layers, while postponing the implementation of specific business logic for a later "Phase 2".
6.  **Import/Package Correction Storm:** A dominant and recurring pattern is the rapid sequence of commits, often seconds apart, dedicated to correcting package declarations and import paths from `com.rakshak.backend.*` to `com.rakshak.rakshak_backend.*` or fixing malformed import statements. This suggests a systematic cleanup after an initial code generation or integration step.

## 2:36:45 PM
The provided log details a series of concentrated code changes within the `rakshak-backend` Spring Boot project, primarily focusing on establishing core infrastructure for user authentication, security, and setting up placeholders for future features. All observed changes occurred on **2/23/2026**, spanning from **12:45 PM to 1:57 PM**, indicating a dedicated development session.

**File-specific Updates:**

*   **`com.rakshak.rakshak_backend.model.Document.java`**: (Changed at 12:45:25 PM, 12:45:33 PM)
    *   Introduced a JPA `@Entity` for `Document` with fields like `id`, `title`, `fileUrl`, `uploadedBy` (ManyToOne relationship with `User`), and `createdAt` (automatically set on persist). The initial change had a duplicated package declaration, which was quickly corrected in the subsequent commit. This entity is marked as a placeholder for "Phase 2" document management.

*   **`com.rakshak.rakshak_backend.dto.*.java`**:
    *   **`RegisterRequest.java`**: (Changed at 12:48:28 PM) Defined a Data Transfer Object (DTO) for user registration, including `name`, `email`, `password`, `examType`, and `city`. It incorporates Jakarta validation annotations (`@NotBlank`, `@Email`, `@Size`).
    *   **`LoginRequest.java`**: (Changed at 12:48:47 PM) Defined a DTO for user login, requiring `email` and `password` with validation annotations.
    *   **`AuthResponse.java`**: (Changed at 12:49:10 PM, 12:50:10 PM) Defined a DTO for authentication responses, including a JWT `token`, `name`, `email`, and `role`. The second commit was a minor save without functional changes.

*   **`com.rakshak.rakshak_backend.repository.*.java`**:
    *   **`UserRepository.java`**: (Changed at 12:50:38 PM, 12:50:50 PM, 12:50:59 PM) Created a Spring Data JPA repository for the `User` entity. It includes methods `findByEmail` and `existsByEmail` crucial for authentication logic. Initial commits involved fixing duplicated package declarations and correcting model imports.
    *   **`DocumentRepository.java`**: (Changed at 12:52:44 PM, 12:53:06 PM, 12:53:33 PM) Introduced a `JpaRepository` for `Document`. A filename typo (`DocumentRepositor.java`) was corrected, followed by fixes to package and import statements to align with the `rakshak_backend` structure. Marked as a placeholder for "Phase 2".

*   **`com.rakshak.rakshak_backend.config.JwtUtil.java`**: (Changed at 12:55:39 PM)
    *   Implemented a utility class for JWT operations. It handles extracting usernames (email) from tokens, generating new tokens with claims and expiration, validating tokens against user details, and decoding the signing key. JWT secret and expiration are configured via `@Value` annotations. (Note: An earlier entry at 12:55:32 PM for `JwtAuthFilter.java` contained this class's code, indicating a temporary misattribution or copy-paste.)

*   **`com.rakshak.rakshak_backend.config.JwtAuthFilter.java`**: (Initial significant changes at 12:57:09 PM, later refactored at 1:57:08 PM, 1:57:14 PM)
    *   Implemented a `OncePerRequestFilter` to intercept HTTP requests. It extracts and validates JWT tokens from the `Authorization` header, loads user details, and sets up the Spring Security context.
    *   **Significant Refactoring (1:57:08 PM onwards)**: The `JwtAuthFilter` was updated to directly inject `UserDetailsServiceImpl` (a concrete implementation) instead of the `UserDetailsService` interface. This was explicitly noted in comments as a fix for a circular dependency issue with `SecurityConfig`.

*   **`com.rakshak.rakshak_backend.config.SecurityConfig.java`**: (Initial significant changes at 12:57:27 PM, later refactored at 1:57:32 PM, 1:57:37 PM)
    *   Configured Spring Security for the application. Key aspects include disabling CSRF, defining public routes (`/api/auth/**`), requiring authentication for all other requests, enforcing stateless session management, and integrating `JwtAuthFilter`. It also defines beans for `AuthenticationManager`, `AuthenticationProvider` (using `DaoAuthenticationProvider`), and `BCryptPasswordEncoder`.
    *   **Significant Refactoring (1:57:32 PM onwards)**: The `UserDetailsService` bean definition was removed from this class and moved to `UserDetailsServiceImpl`. `SecurityConfig` now directly injects `UserDetailsServiceImpl` to resolve a circular dependency, mirroring the change in `JwtAuthFilter`.

*   **`com.rakshak.rakshak_backend.config.WebSocketConfig.java`**: (Changed at 1:02:47 PM)
    *   Configured WebSockets using STOMP. It sets up a simple message broker for `/topic` and application destinations for `/app`, and registers `/ws` as the STOMP endpoint with SockJS fallback. Marked as a placeholder for "Phase 2 real-time features".

*   **`com.rakshak.rakshak_backend.service.AuthService.java`**: (Series of commits between 1:03:22 PM and 1:04:56 PM)
    *   Implemented the core business logic for user registration and login. The `register` method handles email uniqueness, password hashing (using `PasswordEncoder`), user persistence, and immediate JWT issuance. The `login` method delegates to Spring Security's `AuthenticationManager` and then issues a JWT. Multiple subsequent commits corrected various import statements.

*   **`com.rakshak.rakshak_backend.service.*Service.java` (UserService, PostService, CommentService, DocumentService, AIService)**: (Changed between 1:05:58 PM and 1:36:48 PM)
    *   Numerous service classes were introduced as placeholders, each with a `RequiredArgsConstructor` and an injected repository. These are explicitly marked for "Phase 2" or "Phase 2+" functionality, implying a future expansion roadmap. Each saw commits dedicated to fixing package declarations and import statements.
        *   `UserService`: Includes a `getUserById` method.
        *   `AIService`: Mentions integration with external AI APIs for features like MCQ generation.

*   **`com.rakshak.rakshak_backend.controller.*Controller.java` (AuthController, UserController, PostController, CommentController, DocumentController, AIController)**: (Changed between 1:41:46 PM and 1:49:11 PM)
    *   Similar to the services, corresponding controller classes were set up.
    *   **`AuthController.java`**: Implements REST endpoints for `/api/auth/register` (POST) and `/api/auth/login` (POST), delegating to `AuthService`. It also utilizes `@Valid` for DTO validation. Multiple commits corrected package and import issues.
    *   The other controllers (`UserController`, `PostController`, `CommentController`, `DocumentController`, `AIController`) are placeholders with basic `@RequestMapping` and injected service dependencies, awaiting "Phase 2" or "Phase 2+" implementation. All also received updates correcting package and import errors.

*   **`com.rakshak.rakshak_backend.exception.GlobalExceptionHandler.java`**: (Changed at 1:52:35 PM, 1:52:52 PM, 1:53:02 PM, 1:53:20 PM)
    *   An empty class was initially created, then fully implemented as a `@RestControllerAdvice`. It provides global exception handling for `MethodArgumentNotValidException` (returning field errors), `BadCredentialsException` (for login failures), and general `RuntimeException` (for business logic errors), all returning structured JSON responses with appropriate HTTP status codes.

*   **`com.rakshak.rakshak_backend.config.UserDetailsServiceImpl.java`**: (Changed at 1:56:24 PM, 1:56:33 PM, 1:56:38 PM)
    *   Introduced as a standalone component to implement `UserDetailsService`, loading user details by email from `UserRepository`. This was a key architectural change to resolve circular dependencies between `SecurityConfig` and `JwtAuthFilter`.

**Patterns and Recurring Elements:**

1.  **Consistent Package Structure Correction:** A dominant pattern across nearly all files is the initial presence of incorrect or duplicated package declarations (e.g., `package com.rakshak.rakshak_backend.model; package com.rakshak.backend.model;`) or inconsistent imports (`com.rakshak.backend.*` instead of `com.rakshak.rakshak_backend.*`). This was systematically corrected through subsequent commits for almost every newly introduced file, suggesting an initial copy-paste or migration issue that was actively fixed.
2.  **Lombok Usage:** The project extensively uses Lombok annotations (`@Data`, `@Builder`, `@NoArgsConstructor`, `@AllArgsConstructor`, `@RequiredArgsConstructor`) across models, DTOs, services, and configurations to reduce boilerplate code, making the codebase more concise.
3.  **Phased Development Strategy:** Many classes are explicitly labeled as "Placeholders for Phase 2" or "Phase 2+", indicating a clear plan for incremental feature development beyond the initial setup. This includes features like document management, user profiles, posts, comments, and AI integrations.
4.  **Spring Security & JWT-based Authentication:** A significant portion of the work is dedicated to implementing a robust JWT authentication system using Spring Security, encompassing `JwtUtil` for token management, `JwtAuthFilter` for request interception, `SecurityConfig` for defining security rules, and `AuthService` for business logic.
5.  **Dependency Resolution Refactoring:** A notable architectural refinement occurred late in the log (around 1:57 PM) to resolve a circular dependency by creating `UserDetailsServiceImpl` as a standalone component, which `JwtAuthFilter` and `SecurityConfig` then directly inject. This demonstrates attention to good design principles and maintainability.
6.  **Global Exception Handling:** The implementation of `GlobalExceptionHandler` ensures a consistent and structured approach to reporting errors to clients, improving API usability.

## 3:36:45 PM
The provided log details a concentrated period of development activity, primarily focusing on establishing the foundational backend architecture for the `rakshak-backend` application in Java Spring Boot.

**File-Specific Updates:**

*   **`Document.java` (Model):** Initial creation (`2/23/2026, 12:45:25 PM`) as a JPA entity with basic fields like `id`, `title`, `fileUrl`, `uploadedBy` (a ManyToOne relationship to `User`), and `createdAt` with an auto-setting `@PrePersist` hook. It's explicitly marked as a "Placeholder entity for Phase 2". Minor package path correction followed immediately.
*   **DTOs (`RegisterRequest.java`, `LoginRequest.java`, `AuthResponse.java`):** Created around `2/23/2026, 12:48 PM` to `12:50 PM`.
    *   `RegisterRequest` includes fields for user registration (name, email, password, optional examType, city) with `@NotBlank`, `@Email`, and `@Size` validation annotations.
    *   `LoginRequest` contains fields for user login (email, password) with validation.
    *   `AuthResponse` defines the JSON structure returned after successful authentication, including `token`, `name`, `email`, and `role`.
*   **Repositories (`UserRepository.java`, `DocumentRepository.java`, `PostRepository.java`, `CommentRepository.java`):**
    *   `UserRepository.java` was created around `2/23/2026, 12:50 PM`, extending `JpaRepository` and adding `findByEmail` and `existsByEmail` methods crucial for authentication.
    *   `DocumentRepository.java` and other placeholder repositories (`PostRepository.java`, `CommentRepository.java`) were added around `2/23/2026, 12:52 PM` to `1:07 PM`. These are minimal `JpaRepository` interfaces, indicating future development in "Phase 2".
*   **Configuration Files (`JwtUtil.java`, `JwtAuthFilter.java`, `SecurityConfig.java`, `WebSocketConfig.java`, `UserDetailsServiceImpl.java`, `DataSourceConfig.java`):**
    *   **`JwtUtil.java`** (`2/23/2026, 12:55:39 PM`): A core utility for JWT operations, handling token generation, validation, and claim extraction (username, expiration). It sources `secretKey` and `jwtExpiration` from application properties.
    *   **`JwtAuthFilter.java`** (`2/23/2026, 12:57:09 PM`, then `1:57:08 PM`): Implements `OncePerRequestFilter` to intercept HTTP requests, validate JWT tokens from the Authorization header, and set up the Spring Security context. A significant update at `1:57:08 PM` modified it to inject `UserDetailsServiceImpl` directly, resolving a circular dependency with `SecurityConfig`.
    *   **`SecurityConfig.java`** (`2/23/2026, 12:57:27 PM`, then `1:57:32 PM`): Configures Spring Security, defining HTTP authorization rules (e.g., `/api/auth/**` are public, others require authentication), setting session management to stateless, and registering authentication providers (`DaoAuthenticationProvider` using `UserDetailsServiceImpl` and `BCryptPasswordEncoder`). It was updated at `1:57:32 PM` to remove its internal `UserDetailsService` bean and instead inject `UserDetailsServiceImpl` for dependency resolution.
    *   **`WebSocketConfig.java`** (`2/23/2026, 1:02:47 PM`): Configures a STOMP-based WebSocket message broker for real-time features, enabled for `/topic` subscriptions and `/app` destinations, with `/ws` as the endpoint. Commented as a "placeholder ready for Phase 2".
    *   **`UserDetailsServiceImpl.java`** (`2/23/2026, 1:56:33 PM`): Introduced as a standalone `UserDetailsService` implementation to load user details from `UserRepository` by email, specifically created to break a circular dependency in the security configuration.
    *   **`DataSourceConfig.java`** (`2/23/2026, 2:41:56 PM`): Temporarily added to manually configure a HikariCP `DataSource` for Supabase, attempting to handle specific connection properties like the dotted username and `prepareThreshold`. This approach was subsequently abandoned, and the file was marked for deletion as the configuration was moved back to `application.properties`.
*   **Services (`AuthService.java`, `UserService.java`, `PostService.java`, `CommentService.java`, `DocumentService.java`, `AIService.java`):**
    *   **`AuthService.java`** (`2/23/2026, 1:03:22 PM`): Implements the core business logic for user registration (checking email existence, hashing passwords, saving users, and generating JWTs) and login (authenticating credentials via `AuthenticationManager`, fetching user, and issuing new JWTs).
    *   Placeholder services (`UserService.java`, `PostService.java`, `CommentService.java`, `DocumentService.java`, `AIService.java`) were created between `1:05 PM` and `1:36 PM`. These classes are minimal, primarily injecting their respective repositories, and are clearly marked for "Phase 2" or "Phase 2+" feature development (e.g., profile management, feed, AI integration).
*   **Controllers (`AuthController.java`, `UserController.java`, `PostController.java`, `CommentController.java`, `DocumentController.java`, `AIController.java`):**
    *   **`AuthController.java`** (`2/23/2026, 1:41:46 PM`): Defines REST endpoints for user authentication: `/api/auth/register` (POST) and `/api/auth/login` (POST), delegating logic to `AuthService`. It uses `@Valid` for DTO validation.
    *   Placeholder controllers (`UserController.java`, `PostController.java`, `CommentController.java`, `DocumentController.java`, `AIController.java`) were established between `1:46 PM` and `1:49 PM`. These provide basic `@RestController` setups with `@RequestMapping` and `@RequiredArgsConstructor`, ready for "Phase 2" feature endpoints. `UserController` includes a simple `GET /api/users/{id}` endpoint.
*   **`GlobalExceptionHandler.java`** (`2/23/2026, 1:52:52 PM`): Introduced as a `@RestControllerAdvice` to centralize exception handling. It provides structured JSON error responses for `MethodArgumentNotValidException` (validation failures), `BadCredentialsException` (login issues), and general `RuntimeException` (business logic errors).
*   **`application.properties`:** Underwent several significant updates between `2/23/2026, 2:36 PM` and `2:53 PM`. Initial database connection for Supabase (using a session pooler, SSL, specific HikariCP settings) and JWT secret/expiration were configured. There was an intermediate attempt (`2:40 PM` - `2:42 PM`) to separate Supabase connection properties and use a custom `DataSourceConfig`, but this was reverted. The final configuration at `2:53:06 PM` consolidates all Supabase PostgreSQL connection details (host, port, database, username, password, sslmode, prepareThreshold) directly in the JDBC URL, along with HikariCP, JPA/Hibernate, and JWT settings.

**Timestamps of Significant Changes:**

*   **Initial Core Setup (around 12:45 PM - 1:07 PM):** Rapid creation and initial configuration of models, DTOs, repositories, and fundamental security components (`JwtUtil`, `JwtAuthFilter`, `SecurityConfig`, `AuthService`). This period also saw the introduction of placeholder service and repository interfaces.
*   **Controller Layer & Global Exception Handling (around 1:41 PM - 1:53 PM):** Development of the API endpoint layer with `AuthController` and other placeholder controllers, alongside the implementation of `GlobalExceptionHandler` for consistent error responses.
*   **Security Dependency Resolution (around 1:56 PM - 1:57 PM):** Introduction of `UserDetailsServiceImpl` and subsequent refactoring in `JwtAuthFilter` and `SecurityConfig` to resolve a critical circular dependency in the Spring Security setup.
*   **Database Configuration Iterations (around 2:36 PM - 2:53 PM):** Multiple adjustments to `application.properties` to fine-tune PostgreSQL connection for Supabase, including an abandoned attempt at a custom `DataSourceConfig`, culminating in a finalized `application.properties` database setup.

**Patterns and Recurring Elements:**

1.  **Package Name Standardization:** A frequent pattern is the correction of package names, often from `com.rakshak.backend` to `com.rakshak.rakshak_backend`, indicating an initial setup or copy-paste that required consistent refactoring across the codebase.
2.  **Placeholder for Future Phases:** A predominant theme is the use of "Phase 2" or "Phase 2+" comments in numerous classes (models, repositories, services, controllers for documents, posts, comments, AI, user profiles), highlighting an incremental development strategy where authentication and basic structure are established first.
3.  **Lombok for Boilerplate Reduction:** Extensive use of Lombok annotations (`@Data`, `@Builder`, `@NoArgsConstructor`, `@AllArgsConstructor`, `@RequiredArgsConstructor`) across DTOs, models, and Spring components to minimize boilerplate code, promoting cleaner and more concise Java.
4.  **Spring Security with JWT:** A central focus is on implementing a stateless, JWT-based authentication system using Spring Security, involving a custom `JwtUtil`, `JwtAuthFilter`, `SecurityConfig`, and `UserDetailsServiceImpl` for user authentication and authorization.
5.  **Supabase PostgreSQL Integration:** Dedicated efforts are visible in configuring the application to connect to a Supabase PostgreSQL database, specifically addressing requirements like session pooler usage, SSL, and HikariCP pool tuning.